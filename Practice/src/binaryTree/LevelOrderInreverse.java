package src.binaryTree;

/**
 * @author ankit here we are printing the level order of a binary tree in
 *         reverse order Like first 3 level, then 2, then 1
 */
public class LevelOrderInreverse {
	public static void main(String a[]) {
		BinaryTree bt = new BinaryTree();
		TreeNode root = bt.getBinaryTree();
		printLevelOrderInreverse(root);
	}

	private static void printLevelOrderInreverse(TreeNode root) {
		
	}
}
